title: Homepage

options:
  changeStatus: false
  duplicate: false

icon: text

sections:

  header:
    headline: Header
    type: pages
    templates:
      - hero-content
    image: false
    info: "{{ page.intendedTemplate }}"
    create: false
    sortable: false

  content:
    headline: Main Content
    type: pages
    templates:
      - block-text
    image: false
    info: "{{ page.intendedTemplate }}"
    sortable: false
